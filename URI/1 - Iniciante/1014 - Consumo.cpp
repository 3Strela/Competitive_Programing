#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int a;
    double b;
    cin >> a >> b;
    cout << setprecision(3) << fixed << a / b << " km/l" << "\n";

    return 0;
}